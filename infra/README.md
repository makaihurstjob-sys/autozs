# Infrastructure

The local MVP is currently managed by the root `docker-compose.yml`. Add deployment manifests, backup scripts, and production environment templates here as the system moves beyond local development.
